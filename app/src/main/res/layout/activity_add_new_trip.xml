<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusableInTouchMode="true"
    android:clickable="true"
    tools:context=".addNewTrip">

    <EditText
        android:id="@+id/etTripName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Destination"
        android:imeOptions="actionDone"
        android:inputType="text"
        android:singleLine="true"
        app:layout_constraintBottom_toTopOf="@+id/etDepartDate"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="75dp"
        android:layout_marginEnd="75dp"
        />

    <EditText
        android:id="@+id/etDepartDate"
        android:layout_width="132dp"
        android:layout_height="55dp"
        android:hint="Depart"
        android:imeOptions="actionDone"
        android:focusableInTouchMode="false"
        app:layout_constraintBottom_toTopOf="@+id/etArrivalDate"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/etTripName" />

    <EditText
        android:id="@+id/etArrivalDate"
        android:layout_width="132dp"
        android:layout_height="55dp"
        android:focusableInTouchMode="false"
        app:layout_constraintTop_toBottomOf="@+id/etDepartDate"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:hint="Arrival"
        android:imeOptions="actionDone"
        />

    <Button
        android:id="@+id/btnSaveTrip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/etArrivalDate"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:hint="Save"
        />

</androidx.constraintlayout.widget.ConstraintLayout>